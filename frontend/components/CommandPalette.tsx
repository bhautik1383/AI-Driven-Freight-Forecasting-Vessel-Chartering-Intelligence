"use client";
import { useEffect, useState } from "react";
const actions=[['⌂','Command Center','hero'],['◔','Freight Intelligence','freight'],['⌖','Vessel Intelligence','vessels'],['◒','Port Constraints','ports'],['◫','Procurement','procurement'],['✦','Run NaviCast','workflow'],['◌','Ask NaviCast','assistant']];
export function CommandPalette({open,setOpen,run}:{open:boolean;setOpen:(v:boolean)=>void;run:()=>void}){const[q,setQ]=useState('');useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(!open)}if(e.key==='Escape')setOpen(false)};window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[open,setOpen]);if(!open)return null;const filtered=actions.filter(a=>a[1].toLowerCase().includes(q.toLowerCase()));return <div className="modal-backdrop" onMouseDown={()=>setOpen(false)}><div className="command" role="dialog" aria-modal="true" aria-label="Command palette" onMouseDown={e=>e.stopPropagation()}><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Jump to a workspace or run a command…"/><div>{filtered.map(([icon,label,id])=><button key={id} onClick={()=>{setOpen(false);if(id==='workflow')run();else if(id==='assistant')document.getElementById('assistant')?.scrollIntoView({behavior:'smooth'});else document.getElementById(id)?.scrollIntoView({behavior:'smooth'})}}><span>{icon}</span>{label}<kbd>↵</kbd></button>)}</div><footer>Navigate with search <span>Esc</span> to close</footer></div></div>}
